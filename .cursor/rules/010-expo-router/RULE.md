---
alwaysApply: false
---

# Expo Router rules

- All screens/pages live in `app/` and must follow Expo Router conventions.
- Use `_layout.tsx` for stacks/tabs in each route group.
- Keep non-route code in `components/`, `hooks/`, `lib/`, `utils/`.
