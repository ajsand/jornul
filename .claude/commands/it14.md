ITERATION 14 — Auto-tag pipeline wiring (Scratch + Mass Upload → AETS)
SUBAGENTS: journalink-capture (lead), journalink-aets (support), journalink-qa (support)

PRIMARY GOAL
Every ingested item (note/link/file) is auto-tagged offline via AETS during normalize_and_tag jobs.

PREFLIGHT
1) Confirm job runner has normalize_and_tag handler.
2) Confirm AETS tagger functions exist (iter13).

IMPLEMENTATION TASKS
A) Update normalize_and_tag handler:
   - fetch item + media_meta
   - create a text “representation”:
     - for link: domain + url path tokens
     - for file: filename + mime
     - for note: note text
   - call AETS.tagItem(itemId, representation)
B) Add safety:
   - ensure idempotency: re-running job updates assignments, doesn’t duplicate
   - if representation is empty: skip gracefully

QA
- Create items of each type; confirm tags appear automatically on Item Detail.
- Confirm jobs show “tagging done”.

DELIVERABLES
- Offline tagging works end-to-end.
- Commit: "iter14: auto-tag pipeline wired"
