ITERATION 15 — Search v1 (FTS + tag facets)
SUBAGENTS: journalink-db (lead), journalink-ui (support), journalink-qa (support)

PRIMARY GOAL
Make Vault searchable quickly without embeddings yet: SQLite FTS + tag facets.

PREFLIGHT
1) Confirm current “contains match” exists.
2) Verify expo-sqlite supports FTS on the platform build you use.

IMPLEMENTATION TASKS
A) Add FTS table:
   - items_fts(item_id, title, text, url, domain, tags_text)
B) Keep FTS updated:
   - on item insert/update
   - on tag assignment changes
C) Vault UI:
   - search bar queries FTS
   - add tag facet chips from frequent tags in result set

QA
- Search “youtube” returns links; search “stoicism” returns notes tagged accordingly.
- Ensure no duplicate rows.

DELIVERABLES
- Search feels instant and usable.
- Commit: "iter15: search v1 (fts + tag facets)"
