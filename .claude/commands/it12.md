ITERATION 12 — Vault browsing v1 (fast filters: type/date/source)
SUBAGENTS: journalink-ui (lead), journalink-db (support), journalink-qa (support)

PRIMARY GOAL
Create the Vault screen to browse all stored items efficiently with filters.

PREFLIGHT
1) Confirm items/media_meta exist and have data.
2) Confirm navigation includes a Vault tab.

UI REQUIREMENTS
Vault supports:
- List/grid toggle
- Filters: media type (kind), date range (basic), source domain
- Search box (simple contains match for now)
- Tap item → Item Detail screen

IMPLEMENTATION TASKS
A) Vault route: app/(tabs)/vault.tsx
B) Item detail route: app/item/[id].tsx (or similar)
C) DB queries optimized:
   - pagination or “load more”
   - indexes on items.created_at, items.kind, media_meta.source_domain

QA
- 100+ items scroll performance acceptable.
- Filters combine correctly.

DELIVERABLES
- Vault browse is usable and fast.
- Commit: "iter12: vault browsing v1"
