ITERATION 18 — On-device embeddings (dev build requirement + feature flag)
SUBAGENTS: journalink-ai-local (lead), journalink-architect (support), journalink-qa (support)

PRIMARY GOAL
Add optional on-device embeddings to improve tagging/search, behind a feature flag. This likely requires a dev build because native modules aren’t in Expo Go. (Expo explains why packages with native code need dev builds.) 

PREFLIGHT
1) Confirm whether you are using Expo Go or a dev build.
2) If you will add onnxruntime-react-native or similar native ML, plan for dev builds.

IMPLEMENTATION TASKS
A) Add feature flag:
   - settings: “Enable local embeddings”
B) Add embeddings service interface:
   - services/embeddings/IEmbedder.ts
   - default implementation: NOOP or simple hashing vectors
   - optional implementation: ONNX embedder (if feasible in your build)
C) Store embeddings in embeddings table (already exists)
D) Update AETS:
   - if embeddings enabled: use vector similarity to suggest merges/synonyms
   - else: keep AETS v1 heuristics

QA
- App runs with embeddings OFF (default) without any native changes.
- If embeddings ON is implemented, verify it works in dev build and gracefully disables on unsupported builds.

DELIVERABLES
- Embeddings architecture in place, feature-flagged.
- Commit: "iter18: local embeddings scaffold + feature flag"
