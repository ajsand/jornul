ITERATION 13 — Emergent tags v1 (NO preset tags; per-user universe)
SUBAGENTS: journalink-aets (lead), journalink-db (support), journalink-ui (support), journalink-qa (support)

PRIMARY GOAL
Replace any preset/global tags approach with AETS v1: tags are discovered from the user’s content and evolve per-user.

PREFLIGHT
1) Audit code for any hardcoded tag lists. Remove/disable them.
2) Confirm tag tables exist (iter08).

AETS V1 RULES
- No preloaded tag list.
- Tags created from:
  - URL domain tokens (youtube, imdb, spotify)
  - simple keyphrase extraction from notes text
  - filenames / titles from documents
- Store tag confidence and source (auto/manual)

IMPLEMENTATION TASKS
A) Implement services/aets/keyphrase.ts
   - lightweight keyphrase extraction (no LLM): tokenize, remove stopwords, n-grams, score by frequency
B) Implement services/aets/tagger.ts
   - get candidates, normalize slug, create tag if missing
   - assign top N tags to item with score
C) Add Tag management UI (minimal):
   - Settings → Tags: list, rename, merge
   - Item detail: show tags, add/remove manual tag

QA
- Create 10 notes with different topics → confirm emergent tags are created.
- Confirm no “preset tags” appear for brand new user.

DELIVERABLES
- AETS v1 running locally, tags per-user.
- Commit: "iter13: emergent tags v1 (no preset tags)"
